services:
  - type: web
    name: tft-app-backend
    env: go
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: PORT
        value: 8080
      - key: RIOT_API_KEY
        sync: false
